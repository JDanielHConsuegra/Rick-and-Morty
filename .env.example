# ================================
# Rick & Morty GraphQL API - Env
# Rename this file to ".env" and fill in the values.
# Do NOT commit your real .env to source control.
# ================================

# ----- Server -----
PORT=4000
JSON_LIMIT=200kb
# In production set this to your frontend origin, e.g. https://yourapp.com
CORS_ORIGIN=*

# ----- Cache (Redis) -----
# For local Redis (no TLS):
REDIS_URL=redis://127.0.0.1:6379
# For managed Redis with TLS, use rediss:// and include credentials if required:
# REDIS_URL=rediss://user:password@host:port
CACHE_TTL_SECONDS=60

# ----- Database (Sequelize) -----
# Choose one dialect: postgres | mysql
DB_DIALECT=postgres

# PostgreSQL defaults (change as needed)
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=rickmorty
DB_USER=postgres
DB_PASSWORD=postgres

# If your provider requires SSL (e.g., some cloud setups)
DB_SSL=false

# Optional: test database name used by sequelize-cli in "test" env
DB_NAME_TEST=rickmorty_test

# ----- Notes -----
# - If using MySQL:
#   DB_DIALECT=mysql
#   DB_PORT=3306
# - Swagger reads /docs.json and uses the current origin, no need to set a base URL.
# - After editing .env, restart the server so changes take effect.
